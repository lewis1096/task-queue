apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: taskqueue
  POSTGRES_PASSWORD: taskqueue-dev-password
  POSTGRES_DB: taskqueue
  DATABASE_URL: "postgresql://taskqueue:taskqueue-dev-password@postgres:5432/taskqueue"
